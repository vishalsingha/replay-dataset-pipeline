``src.backends`` — Generation Backends
======================================

Thin abstraction over vLLM local inference and OpenAI-compatible APIs.
Backends are pluggable — the committee config determines which backend
each generator/judge uses.

.. automodule:: src.backends
   :members:
   :undoc-members:
   :show-inheritance:
